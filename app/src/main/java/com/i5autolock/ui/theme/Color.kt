package com.i5autolock.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Ioniq 5 inspired palette — refreshed for a premium, "electric performance" feel.
 *
 * The identity stays keyed to Hyundai's Ioniq 5 exterior/interior names, but the tones are
 * deepened and given more chroma so the app reads as a modern EV companion rather than a flat
 * utility. Surfaces lean into layered near-blacks with a teal undertone (dark) and a cool,
 * pearlescent off-white (light).
 *
 * Primary   → Digital Teal Green Pearl (Ioniq 5's launch signature)
 * Secondary → Cyber Sand / champagne warmth
 * Tertiary  → Electric Lime / Gravity Gold accent for energy & charge states
 * Neutrals  → Shooting Star Silver (light) / Phantom Black Pearl (dark)
 * Error     → Ultimate Red
 */

// ── Primary — Digital Teal Green Pearl ──────────────────────────────
val DigitalTeal = Color(0xFF32D6C0)
val DigitalTealBright = Color(0xFF5FF0DA)
val DigitalTealDeep = Color(0xFF00302C)
val DigitalTealSoft = Color(0xFFB2F5EA)
val DigitalTealMuted = Color(0xFF0A4A44)

// ── Secondary — Cyber Sand Beige ────────────────────────────────────
val CyberSand = Color(0xFFD8C39A)
val CyberSandDeep = Color(0xFF2E2410)
val CyberSandSoft = Color(0xFFF6ECD6)

// ── Tertiary — Electric Lime (charge / energy accent) ───────────────
val ElectricLime = Color(0xFFB6F24A)
val ElectricLimeDeep = Color(0xFF1F2E00)
val ElectricLimeSoft = Color(0xFFE7FFC2)

// Gravity Gold kept for warm highlights (12V, warnings).
val GravityGold = Color(0xFFE0A93B)
val GravityGoldDeep = Color(0xFF3A2600)
val GravityGoldSoft = Color(0xFFFFE2A6)

// ── Error — Ultimate Red ────────────────────────────────────────────
val UltimateRed = Color(0xFFFF5A63)
val UltimateRedSoft = Color(0xFFFFDAD8)
val UltimateRedDeep = Color(0xFF410007)

// ── Neutrals — Light (Atlas White / Shooting Star Silver) ───────────
val AtlasWhite = Color(0xFFF3F6F5)
val AtlasSurface = Color(0xFFFBFDFC)
val ShootingStarLight = Color(0xFFE4EBE8)
val ShootingStarLightHigh = Color(0xFFEDF2F0)
val OnAtlasWhite = Color(0xFF0C1614)
val OutlineLight = Color(0xFF6E807B)
val OutlineLightSoft = Color(0xFFC4D1CD)

// ── Neutrals — Dark (Phantom Black Pearl, layered) ──────────────────
val PhantomBlack = Color(0xFF060B0A)
val PhantomBlackElevated = Color(0xFF0E1615)
val PhantomBlackVariant = Color(0xFF16211F)
val PhantomBlackHigh = Color(0xFF1F2C29)
val OnPhantomBlack = Color(0xFFE6F0ED)
val OutlineDark = Color(0xFF54655F)
val OutlineDarkSoft = Color(0xFF2A3835)
